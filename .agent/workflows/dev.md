---
description: Start development for both client and server
---

// turbo
1. Start the client dev server:
   `cd client; npm run dev`

// turbo
2. Start the server:
   `cd server; python -m uvicorn main:app --host 0.0.0.0 --port 5000 --reload`
